env:
  RUNNER_SCRIPT: /.buildkite/setup.sh
  
steps:
  - trigger: slave-pipeline
    build:
      message: "Triggered by Master Pipeline"
      commit: "HEAD"
      branch: "main"
  
  - wait
  
  
  - label: "Unblock Slave Pipeline"
    commands:
      -  $RUNNER_SCRIPT run
